# 新技术设计方案（索引 · 双版本）

> 日期：2026-08-31｜本文件为索引。设计方案按用途拆为两个版本，**共用同一套场景参数与限制基线**，内容不矛盾、互为详略。
> 配套决策记录：`docs/技术决策确认.md`（2026-09-01 锁定——接入侧分组+调度优先、安全侧方向 C 混合、切换侧终端携带令牌）。本索引及 A/B 两版若与其冲突，以决策记录为准。

## 两版对照

| | **A · 计划书对齐版** | **B · 论文级聚焦版** |
|---|---|---|
| 文件 | `docs/设计方案A_计划书对齐版.md` | `docs/设计方案B_论文级聚焦版.md` |
| 用途 | 申报、中期、结题、答辩——与计划书逐条对账 | 技术攻关与论文产出——核心机制做深做实 |
| 范围 | 做全：五大模块、三层架构、四维创新（快/安全/温度/演示） | 做深：只聚焦三个核心痛点（接入风暴/切换中断累积/星上受限） |
| 核心机制 | M1 安全快速接入（四方向）+ M2 预测式无缝切换（五机制） | LAP 轻量两步接入协议 + PTHO 预拓扑驱动无缝切换 |
| 特色内容 | 空基弹性扩展层、生存优先调度、历史灾害复盘、空基增益实验 | 预拓扑表（Contact Plan）设计、复杂度/开销量化分析、消融与失配诚实性实验、论文叙事线 |
| 共同遵守 | 场景参数（§二基线一致）、星上轻量化、不做数据预处理、无核心网、Rel-17 基线、仅仿真验证、物理层指纹不进仿真实现（仅威胁分析论证） | 同左 |

## 执行建议

- **B 版先行**：本学期（2026.9–12）按 B 版实现 LAP + PTHO 两条机制并产出对比数据——它是 A 版的技术内核，也是论文素材。
- **A 版补齐**：B 版机制跑通后，把空基增益实验、生存优先调度、复盘场景包作为扩展项叠加，支撑中期/结题的对账口径。
- 若评审质疑"方案与计划书不符"：出示 A 版（承诺全保留）；若质疑"工作量是否可控"：出示 B 版（内核先行、扩展可裁剪）。

## 接口与决策文档（实现契约，先于编码）

- `docs/技术决策确认.md` — 三招决策的权威记录（接入/认证/切换 + Q4 双根模型 + 风险 R1~R6）。
- `docs/仿真接口约定.md` — **T1 必交接口契约**（2026-09-01 建）：统一认证上下文结构(Q13)、场景配置 JSON、CSV trace 字段、JSON 统计字段、9 项统一指标。
- `docs/T4协议伪代码草图.md` — **实现骨架**（2026-09-01 建，晚回填决议）：首联发证/切换重连状态机 + MsgA/MsgB 字段布局；Q1~Q5 算法已决议（EC-Schnorr CL-PKC / 每次切换轮换 / 星下发种子+单向递增 / 复用星座根 kid），落实 Q6/Q9。

## 版本沿革

- v1（2026-08-31 早）：初版六模块设计——内容全但发散。
- v2（2026-08-31 晚）：按技术负责人意见收敛为聚焦版。
- v3（2026-08-31 晚）：应要求拆分为 A（≈v1 的计划书口径，重写过）+ B（≈v2 的论文级深化，新增预拓扑与开销论证），本索引取代单文件方案。
- v4（2026-09-01）：同步 Q4 双根模型至 A/B/综述；新建 `仿真接口约定.md` 完成 Q13/Q15 接口先行项。
- v5（2026-09-01 晚）：Q1~Q5 全部决议回填（EC-Schnorr CL-PKC / 每次切换轮换假名 / 星下发种子+单向递增哈希链 / 复用星座根 kid 签名）；接口约定与骨架同步；待决仅余 Q14 暂挂 + Q7~Q12 提议默认。
