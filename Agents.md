# Agents.md — AI 智能体协作技术实施规划

## 项目：面向应急救灾的低轨卫星-地面融合组网快速接入系统

> 本文档面向技术负责人，规划如何用 AI（编程智能体 + 检索智能体）完成本项目的技术工作：**代码编写、数据搜集、仿真搭建、结果分析**。依据 Note.txt / Text.txt 与《大创计划书初稿》整理。
> v3：加入"工具选择与辅助能力判断"框架——每次给 AI 派任务时，先判断用什么工具、是否需要加载 skill、是否需要连接器或专家（Expert）辅助。
>
> **v4（2026-08-31 修订）**：
> ① 补齐 v3 遗漏的两块任务——**T4 安全认证机制**（计划书核心创新之一）、**T9 空基扩展层**；另将 **T2 场景与轨道建模**从 T1 中拆出独立；
> ② 仿真选型锁定为 **ns-3（协议级）+ Python（推演/数据）+ Web（可视化）三栈分工**，不再是"ns-3 vs 自研"二选一（计划书已定稿）；
> ③ 新增「一·B 仿真系统技术栈分工」一节，按技术栈划分职责边界与数据接口；
> ④ 空基段定位为**可开关的弹性扩展层**，v1 默认关闭，先交付"卫星直连"基线。

---

## 〇、核心目标与场景边界（v5 明确）

> **v5 修订要点**：基于"项目体量偏大、物理层辅助认证等方法不符合纯仿真环境条件"的复盘，本节集中固化"项目到底做什么、在什么条件下做"，作为所有任务卡**「背景」字段的唯一权威来源**。物理层指纹/CSI 认证、跨域 PKI、群组聚合等超纲内容一律移出主线（见 §0.4）。

### 0.1 一句话目标
面向灾后地面公网（含核心网、信关站）**完全瘫痪**的应急场景，依托低轨卫星星座，让**海量、突发**的地面终端快速接入卫星网络，并以"可预测切换"实现切换低中断；全部以**仿真平台**完成验证与演示。

### 0.2 场景五要素（仿真建模输入）
| 要素 | 设定 | 来源 |
|---|---|---|
| 网络条件 | 灾后地面公网/核心网/信关站全瘫，终端只能直连卫星 | Note.txt |
| 接入负载 | 海量终端**集中、突发**发起接入（突发风暴） | Note.txt / Text.txt |
| 信道与运动 | LEO 约 7.5 km/s，多普勒大、传播时延 20~50 ms | 物理约束 |
| 终端能力 | 终端有 GNSS 定位、已知卫星星历（可做开环预补偿） | Text.txt |
| 星上约束 | 星上算力/存储有限，不堆重型机制；不做数据预处理 | Text.txt 第 6 点 |

### 0.3 环境条件约束（四条，越界即返工）
1. **纯仿真验证**：仅 ns-3（协议级）+ Python（推演/数据）+ Web（可视化），不接触真实卫星硬件、不依赖真实公网。
2. **无核心网**：认证/切换不得假设核心网（AUSF/UDM/HSS）可用，一切本地化到卫星载荷。
3. **星上轻量化**：星上机制满足"不增握手轮次、不显著增信令、计算轻量"三重约束；把"认证引入额外时延"列为独立指标。
4. **物理层指纹不进仿真**（★v5 新增）：RF fingerprinting / CSI / SRS 类物理层认证**不作为 ns-3 仿真实现模块**——ns-3 物理层为基于模型的抽象、无真实可采信道指纹，且本项目无硬件/实测数据支撑其性能数字；该方向仅在"威胁分析"中作为论证素材（说明单靠密码层不够、需融合），**不写仿真代码、不计入工作量**。

### 0.4 范围收敛清单（非目标 · 一律不进主线）
| 方向 | 处理 | 理由 |
|---|---|---|
| 物理层指纹/CSI/SRS 认证实现 | **不实现**（仅威胁分析论证） | ns-3 无真实指纹、无硬件/实测、算力重，违背轻量 |
| 跨域认证（DCA / P³CDA） | **不做** | 面向多运营商/多域漫游，本项目单系统单域 |
| 在轨 PKI / In-Orbit Trust | **不做** | 完整证书体系过重，ns-3 难落地 |
| 群组聚合切换认证（万级批量） | **不做** | 演示场景成百上千级，聚合收益不显著、实现复杂 |
| 区块链 / 共识机制 | **不做**（已排除） | 违背星上轻量化约束 |
| Dolev-Yao 形式化证明 | **不做**（改攻击树/场景级威胁分析） | 形式化验证超越大创仿真范围 |

> 上述归入**「未来工作 / 可扩展」**，不在 T1~T12 主线内；如后续确要做，须先升级任务卡并评估对 §0.3 环境约束的冲击。

---

## 一、技术任务拆解

v4 已**按执行顺序重排为连续编号**。**加粗行为 v4 新增任务**。

| # | 任务 | 说明 | 技术栈 | 对应技术要点 |
|---|------|------|--------|--------------|
| T1 | 仿真环境搭建 | **ns-3 + Python 双轨骨架**：ns-3 侧装卫星/NTN 模块，Python 侧搭轨道模型与事件调度框架 | ns-3 + Python | 基础平台 |
| **T2** | **场景与轨道建模** | **Walker 星座/TLE 导入、灾害场景参数包（汶川/河南暴雨）、终端分布与突发强度、终端危险度标签** | **Python** | **计划书模块一** |
| T3 | 接入握手协议实现 | 实现并优化终端-卫星接入握手流程，降低时延（四步→两步预补偿） | ns-3 | Note.txt：优化接入握手协议 |
| **T4** | **安全认证机制实现** | **星上本地验证的轻量凭证认证**（CLC 无证书签名 或 HMAC-哈希链 + 动态匿名标识/假名轮换）；伪造终端/恶意无人机拦截仿真（**不含物理层指纹实现**，见 §0.3/§0.4） | **ns-3** | **计划书模块三·优化方向四** |
| T5 | 可预测切换模块 | 基于星历预测下一可连卫星，提前打包认证信息（先建后断） | ns-3 | Text.txt 第 1、4 点 |
| T6 | 切换决策模块 | 依据链路剩余可见时间、稳定性阈值决定是否切换（抑制乒乓） | ns-3 | Text.txt 第 3 点 |
| T7 | 快速重连确认 | 新卫星一次关键信息比对即确认（数据包关键字段位置/内容固定） | ns-3 | Text.txt 第 2 点 |
| T8 | 业务连续性保障 | 切换过渡快且平滑，区分业务类型（指挥话音/灾情图像/报平安短信）的传输需求 | ns-3 | Text.txt 第 5 点 |
| **T9** | **空基扩展层（可开关）** | **无人机/飞艇中继与汇聚节点建模**；仅做配置化节点（数量/覆盖半径/驻留时长），不做编队控制与轨迹规划；**v1 默认关闭** | **ns-3（配置层）** | **计划书模块二** |
| T10 | 数据搜集与 baseline | 搜集真实星座参数（Starlink/OneWeb 星历、链路预算公开数据）、传统接入方案作为对照 | Python | 实验对比 |
| T11 | 指标评估与可视化 | 接入时延、切换中断时长、重选星耗时、成功率、**伪造终端拦截率**，生成对比图表 | Python | 答辩材料 |
| T12 | 演示 Demo | 推演驱动 + Web 可视化界面（ECharts / Three.js） | Web | 比赛演示 |

<details>
<summary>v3 → v4 编号映射（旧文档引用对照用）</summary>

| v3 | v4 | 变化 |
|---|---|---|
| T1 仿真环境搭建 | T1 | 内容更新为双轨骨架 |
| — | T2 场景与轨道建模 | **新增**（原并入 T1，v4 拆出独立） |
| T2 接入握手协议 | T3 | 编号后移 |
| — | T4 安全认证机制 | **新增**（v3 遗漏，计划书核心创新） |
| T3 可预测切换 | T5 | 编号后移 |
| T4 切换决策 | T6 | 编号后移 |
| T5 快速重连确认 | T7 | 编号后移 |
| T6 业务连续性 | T8 | 编号后移 |
| — | T9 空基扩展层 | **新增**（可开关，v1 默认关闭） |
| T7 数据搜集与 baseline | T10 | 编号后移 |
| T8 指标评估与可视化 | T11 | 编号后移，指标新增"拦截率" |
| T9 演示 Demo | T12 | 编号后移 |

</details>

**边界约束**：
1. 星上资源有限（仿真中需建模算力/存储约束）；
2. 不做数据预处理；
3. 仅仿真验证，不接触真实卫星硬件；
4. **空基段不做编队控制、轨迹规划与飞行动力学**——仅作为可配置中继/汇聚节点建模；v1 默认关闭，进度紧张时可整体裁剪，不影响"接入 / 认证 / 切换"三条主线交付。
5. **物理层指纹/CSI/SRS 认证不进入 ns-3 仿真实现**（见「〇、核心目标与场景边界 §0.3/§0.4」）：仅在威胁分析章节论证"为何需与密码层融合"，不写仿真代码、不计入工作量。

---

## 一·B、仿真系统技术栈分工（v4 新增）

仿真部分**按技术栈划分**为三块（ns-3 / Python / Web），每块有明确职责边界与交付物，避免"谁都能改、谁都不负责"。

```
  ┌──────────────────────────────────────────────┐
  │ Python 侧（推演 / 数据 / 供数）                 │
  │ 星座轨道 · 可见性预计算 · 灾害场景参数包         │
  │ 实验批量驱动 · trace 解析 · 指标统计            │
  └───────┬──────────────────────────▲───────────┘
          │ 场景配置(JSON)            │ trace(CSV)
          ▼                          │
  ┌──────────────────────────────────────────────┐
  │ ns-3 侧（协议级严谨仿真）                      │
  │ RACH · 接入握手 · 星上认证 · 切换判决           │
  │ 信道模型 · 移动性 · 链路级指标采集              │
  └──────────────────────────────────────────────┘

  Python 侧统计结果(JSON)
        │
        ▼
  ┌──────────────────────────────────────────────┐
  │ Web 侧（可视化 / 演示）                        │
  │ 星座运行 · 波束覆盖 · 接入/切换事件 · 时延图表  │
  └──────────────────────────────────────────────┘
```

### 1. 逻辑分层 → 技术栈映射

| 逻辑分层 | 内容 | 技术栈 | 对应任务 |
|---|---|---|---|
| L1 场景与轨道层 | Walker 星座 / TLE、灾害场景参数包、终端分布与危险度标签 | **Python** | T2 |
| L2 信道与链路层 | 星地可见性、链路预算、多普勒频偏、定时提前量 TA、SNR/丢包 | **ns-3**（模型）+ Python（预计算） | T1、T2 |
| L3 协议状态机层 | 接入握手、星上认证、预测式切换 | **ns-3** | T3、T4、T5~T8 |
| L4 评估与数据层 | trace 输出、指标统计、实验批量驱动、结果数据集 | **ns-3 → Python** | T10、T11 |
| L5 可视化推演层 | 数字孪生推演、Web 展示、历史灾害复盘 | **Python + Web** | T12 |

### 2. 三栈职责边界（**越界即返工**）

| 技术栈 | 负责 | **不负责** | 交付物 | 主要负责人 |
|---|---|---|---|---|
| **ns-3** | 协议级严谨仿真：RACH 流程、接入握手、认证过滤、切换判决、链路级指标采集 | 不做可视化、不生成场景参数、不做图表 | 仿真模块 + trace(CSV) | 姚世宸（方案）+ 王泽诚（实现） |
| **Python** | 轨道/可见性预计算、灾害场景参数包、实验批量驱动、trace 解析与统计、给前端供 JSON | **不实现协议状态机** | 脚本 + 数据集 + JSON | 王维（轨道）+ 姚世宸（指标口径） |
| **Web** | ECharts / Three.js 可视化、演示界面、历史复盘动画 | **不跑仿真、不做统计** | 前端页面 | 待定 |

### 3. 接口约定（**先定接口再开工，避免后期对接返工**）

| 接口 | 方向 | 格式 | 约定 |
|---|---|---|---|
| 场景配置 | Python → ns-3 | JSON / 命令行参数 | 星座参数、终端分布、突发强度、空基节点配置（开关） |
| 仿真 trace | ns-3 → Python | **CSV** | 每行一条事件，字段表在 `docs/仿真接口约定.md` 定义（**待建，T1 阶段必须产出**） |
| 统计结果 | Python → Web | **JSON** | 字段名与 CSV 指标列严格对齐 |

**统一指标命名**（三方共用，写死在接口文档里）：
`接入时延(avg/p95)`、`接入成功率`、`RACH 吞吐量`、`切换中断时间`、`切换成功率`、`乒乓切换率`、`预测失配率`、`伪造终端拦截率`、`认证引入额外时延`。

### 4. 空基段处理原则（用户已确认）

1. 实现为**可开关的配置模块**，参数仅三项：节点数量、覆盖半径、驻留时长；
2. **v1 默认关闭**，先交付"卫星直连"基线（含 T3/T4/T5~T8 全部主线功能）；
3. 卫星直连基线跑通并产出对比数据后，再开启空基做**空基增益专项实验**（仅卫星直连 / 卫星+地面汇聚 / 卫星+地面汇聚+空基中继 三配置对比）；
4. 进度紧张时整体裁剪，不影响主线交付。

### 5. AI 参与的边界（写进任务卡）

| 内容 | AI 参与度 | 说明 |
|---|---|---|
| 协议/认证**方案设计** | ❌ 不参与 | **必须由人主导**（姚世宸），AI 只做实现与验证（认证实现**不含物理层指纹模块**，见 §0.4）——答辩命门 |
| ns-3 模块代码实现 | ✅ 高度参与 | 人给方案与伪代码，AI 写实现 + 单元测试 |
| Python 轨道/场景脚本 | ✅ 高度参与 | skyfield/sgp4、参数包生成、trace 解析 |
| Web 可视化 | ✅ 高度参与 | 人定交互草图，AI 写前端 |
| 数据搜集 | ✅ 参与但需溯源 | 必须给可访问 URL，无源数据拒收 |
| 实验结论解读 | ⚠️ 有限参与 | AI 可出图出数，**结论由人下** |

---

## 二、工具与辅助能力选择框架（新增）

派任何任务给 AI 之前，先过一遍这个判断流程：

```
收到任务
  │
  ├─① 这是什么类型？
  │    ├─ 查资料/搜数据 ──→ 内置 WebSearch / WebFetch 够不够？
  │    │                     ├─ 够 → 直接用
  │    │                     └─ 不够（专业数据库、需登录）→ 查 skill / 连接器
  │    ├─ 写代码/跑仿真 ──→ 本地文件工具 + 受管 Python 环境
  │    ├─ 画图/做表 ─────→ 快速预览用内联可视化；交付物生成文件
  │    ├─ 写文档 ────────→ 直接写 md 文件
  │    └─ 需要真实软件 ──→ 人工操作（STK 等），AI 只出脚本/步骤
  │
  ├─② 能力缺口判断：AI 内置能力做不到吗？
  │    ├─ 做不到 → 先搜可用 skill（如 Office 处理、专业领域工具）
  │    ├─ 有匹配 → 加载 skill 后执行
  │    ├─ 无匹配 → 找连接器/MCP 工具；都没有 → 明确告知人工兜底
  │    └─ 注意：若是"专业能力"缺口（深度研究、专业判断、多角色评审）
  │            而非"工具"缺口 → 走专家（Expert）通道，见下方说明
  │
  ├─③ 任务规模判断：
  │    ├─ 单文件/单函数 → AI 直接做
  │    └─ 多模块/探索性 → 拆小，或派子代理并行调研
  │
  └─④ 产出形式判断：
       ├─ 看一眼就行 → 内联展示（图表/预览）
       └─ 要交付/复用 → 写成文件（代码、数据、文档）
```

### 专家（Expert）使用规则

专家是"带专业角色、方法和工作流"的会话模式，与 skill/连接器互补：

| 对比 | Skill | 连接器（Connector） | 专家（Expert） |
|------|-------|---------------------|----------------|
| 本质 | 一段可复用的操作流程/命令 | 外部数据与服务的接口 | 一个专业角色 + 方法论 |
| 解决 | "怎么做"的流程问题 | "拿什么数据/调什么服务" | "以什么专业视角判断" |
| 例子 | Excel 处理、PPT 生成 | 专利检索库、金融数据 | 通信协议评审、论文写作指导 |

**本项目建议使用专家的场景**：
- **方案设计评审**（T3 接入 / T4 认证 / T5~T7 切换）：协议与认证方案定稿前，启用通信/网络领域专家角色做一轮方案评审，挑漏洞
- **安全威胁建模**（T4）：认证机制设计阶段，启用安全方向专家审**攻击树/场景级威胁模型**（窃听/重放/伪造/篡改；物理层指纹仅作"需与密码层融合"的论证素材，不进仿真）
- **文献调研**（T10）：需要深度研究类专家做系统性文献综述，而非简单搜索
- **论文/申报书撰写**：学术写作专家把关结构与表达
- **答辩准备**：可让专家扮演"刁难评委"做模拟问答

**使用原则**：
1. 专家与专家团**同时只启用一个**，按当前任务阶段切换（设计期→评审专家；写作期→写作专家）。
2. 需要专家时由 AI 推荐真实候选、以卡片形式由你确认启用，不凭空指定。
3. 专家负责"判断与建议"，具体产出（代码/数据）仍回到 A1~A3 的流程执行。
4. 工具类缺口优先 skill/连接器；判断类缺口才上专家，别拿专家当搜索引擎用。

### 各类任务的默认工具映射

| 任务类型 | 首选工具 | 需要 skill/外部工具的场景 | 人工兜底 |
|----------|----------|---------------------------|----------|
| 文献/数据检索 | WebSearch、WebFetch | 专业金融/专利/论文库 → 对应连接器；需登录的资源 | 学校数据库手动下载 |
| 星历数据获取 | WebFetch 抓取 TLE（CelesTrak） | 大批量历史星历 → 写脚本批量下载 | 手动导出 |
| **ns-3 侧开发**（T1、T3~T9） | 文件工具 + 本机/WSL2 的 ns-3 环境（C++） | 环境配置与编译错误 → 派子代理专项排查 | **本机装 ns-3（人工）**，AI 只写模块代码与 CMake 配置 |
| **Python 侧开发**（T2、T10、T11） | 文件工具 + 受管 Python venv（skyfield/sgp4/pandas/matplotlib） | — | 环境配置 |
| **Web 侧开发**（T12） | 文件工具 + 受管 Node（ECharts / Three.js） | — | 演示脚本与排练 |
| 代码调试 | 本地运行 + 读报错迭代 | 复杂 bug → 派子代理专项排查 | 人工 review |
| 数据处理/清洗 | Python 脚本（pandas/numpy） | Excel 深度操作 → 表格类 skill | 手工核对 |
| 图表生成 | 快速看 → 内联图表；交付 → matplotlib 出 PNG/PDF | 交互式大屏 → 前端代码 | PPT 手排 |
| 文档/PPT 产出 | md 文件；正式文档 → 文档类 skill | 答辩 PPT → PPT 生成 skill | 人工排版 |
| 仿真软件操作 | AI 写配置文件/操作脚本 | STK、OPNET 等 GUI 软件 AI 无法直接操作 | 人工按脚本操作 |

### 判断原则

1. **内置工具优先**：能用搜索、文件、代码执行解决的，不引入额外依赖。
2. **能力缺口先搜 skill**：遇到内置能力做不到的（Office 文件、专业领域、外部平台），先查有没有现成 skill，而不是直接说做不了。
3. **连接器按需启用**：需要第三方数据/服务时才推荐连接，平时不挂多余工具。
4. **判断类缺口上专家**：方案评审、深度研究、写作把关等专业判断场景，启用对应专家角色；同时只启用一个。
5. **工具选择写进任务卡**：派任务时明确"用什么工具、产出什么格式"，避免 AI 自己绕路。
6. **环境隔离**：Python 包一律装进受管 venv，Node 包装进受管 workspace，不污染本机。

---

## 三、AI 智能体分工

```
                    ┌─────────────────────┐
                    │  技术负责人（你）      │
                    │  任务拆解/工具选择/验收│
                    └──────────┬──────────┘
           ┌───────────────────┼───────────────────┐
           ▼                   ▼                   ▼
   ┌───────────────┐   ┌───────────────┐   ┌───────────────┐
   │ A1 检索智能体   │   │ A2 编程智能体   │   │ A3 分析智能体   │
   │ 数据/文献搜集   │   │ 代码生成与调试  │   │ 结果分析/图表   │
   │ 工具：搜索/抓取 │   │ 工具：文件+venv │   │ 工具：Python绘图│
   └───────────────┘   └───────────────┘   └───────────────┘
```

> A1~A3 是**能力角色**，与技术栈正交：同一个 ns-3 任务里，既可能用到 A1（查 3GPP 标准），也可能用到 A2（写模块代码）。派单时按能力选角色，按技术栈定目录。

### A1 检索智能体（数据搜集）
- 工具：WebSearch / WebFetch 为主；专业库走连接器
- 产出：结构化数据文件（CSV/JSON）+ 文献摘要清单
- **本项目重点**：T2 星历 TLE 与星座参数、T10 baseline 数据与 3GPP NTN 标准条款
- **验收**：格式规范、来源可溯源、字段齐全

### A2 编程智能体（代码编写）
- 工具：文件读写 + 受管 Python venv（Python 侧）；ns-3 侧需本机/WSL2 环境（C++）
- 要求：模块可独立运行、有单元测试、有参数配置入口
- **本项目重点**：T1 双轨骨架、T3 接入、T4 认证、T5~T8 切换、T9 空基（ns-3）；T2 轨道与场景（Python）；T12 前端（Web）
- **验收**：跑通 + 测试通过 + README

### A3 分析智能体（结果处理）
- 工具：Python（matplotlib/pandas）出交付图表；内联图表做快速预览
- 产出：实验结果图 + 数据表格 + 结论摘要
- **本项目重点**：T11 指标对比图（含伪造终端拦截率）、历史灾害复盘数据
- **验收**：图表规范（中文标注）、结论与数据一致

---

## 四、人机协作工作流（单个模块的标准流程）

```
你：编写任务卡（背景+输入输出+验收标准+指定工具）
        │
        ▼
你：过工具选择框架（第二节）→ 确认是否要 skill / 连接器 / 专家
        │
        ▼
AI：生成初版（代码/数据/分析）
        │
        ▼
你：审查 → 反馈修改意见（可迭代 2~3 轮）
        │
        ▼
你：本地运行验收 → 通过后提交 Git
        │
        ▼
记录：prompt 与关键决策存入 prompts/（可复用）
```

**关键原则**：
1. **任务卡先行**——必须包含：上下文背景、明确输入输出格式、验收标准、约束条件、**指定工具**。
2. **小步快跑**——单次任务控制在"一个函数/一个模块/一份数据集"粒度。
3. **人工验收兜底**——代码必须本地跑通、数据必须抽查来源后才入库。
4. **prompt 资产化**——好用的 prompt 沉淀到 prompts/，全队复用。
5. **工具用错及时纠偏**——发现 AI 绕路（如该抓数据却在编数据），立即在任务卡里锁死工具和数据源。

---

## 五、各任务的人机分工与工具选择

| 任务 | 技术栈 | AI 承担（工具） | 你承担 |
|------|--------|-----------------|--------|
| T1 仿真环境 | ns-3 + Python | 生成双轨骨架、依赖清单（文件工具 + venv） | 环境验收、**敲定 trace 接口字段表** |
| T2 场景与轨道建模 | Python | 轨道模型与灾害场景参数包代码（skyfield/sgp4 + pandas） | 灾害场景参数取值、终端分布假设 |
| T3 握手协议 | ns-3 | 协议流程代码、报文结构定义（venv） | **协议优化方案设计（核心创新，人来定）** |
| T4 安全认证 | ns-3 | 凭证/令牌机制代码实现、伪造终端攻击仿真脚本（venv） | **认证方案设计、威胁模型确认（核心创新）** |
| T5 预测切换 | ns-3 | 星历解析、可见性预测（venv + skyfield/sgp4 库） | 预测窗口等关键参数确定 |
| T6 切换决策 | ns-3 | 决策逻辑代码（venv） | 阈值设定与调优 |
| T7 重连确认 | ns-3 | 比对字段设计代码（venv） | 安全字段选取确认 |
| T8 业务保障 | ns-3 | 多业务 QoS 调度代码（venv） | 业务模型定义（指挥话音/灾情图像/报平安） |
| T9 空基扩展（可关） | ns-3 | 可配置中继/汇聚节点模块（venv） | 是否启用、参数取值；**可整体裁剪** |
| T10 数据搜集 | Python | 检索、抓取、清洗（WebSearch/WebFetch + pandas） | 来源可信度判断、抽查 |
| T11 指标可视化 | Python | 绘图代码、图表生成（matplotlib）；快速预览用内联图表 | 图表结论解读 |
| T12 Demo | Web | 前端界面代码（Node 受管环境） | 演示脚本编写与排练 |

> **核心算法/协议创新点（T3 接入、T4 认证、T5~T7 切换的方案设计）必须由人主导，AI 只做实现与验证——这是答辩的命门。**
> 派单时务必在任务卡里写死："本任务只实现，不修改方案；方案以 `docs/新技术设计方案.md` 为准。"

---

## 六、数据管理规范

按技术栈分目录，与第一节的 T1~T12 一一对应：

```
NationalCreation1/
├── data/
│   ├── raw/            # AI 搜集的原始数据（星历 TLE、论文参数）
│   ├── processed/      # 清洗后的结构化数据、灾害场景参数包
│   └── sources.md      # 数据来源清单（URL + 日期 + 用途 + 获取工具）
├── src/
│   ├── ns3/            # ns-3 侧：接入握手、星上认证、切换判决、信道/移动性模型
│   ├── python/         # Python 侧：轨道/可见性、场景参数化、实验驱动、trace 解析
│   └── web/            # Web 侧：ECharts / Three.js 可视化前端
├── results/
│   ├── traces/         # ns-3 原始 trace（CSV）
│   ├── stats/          # Python 统计结果（JSON / CSV）
│   └── figures/        # 图表成品（PNG / PDF，中文标注）
├── prompts/            # 沉淀的优质 prompt（含工具指定）
└── docs/
    ├── 仿真接口约定.md   # ★ 三栈共用的 trace 字段表 + 统一指标命名（T1 阶段必须产出）
    └── 设计文档、会议纪要
```

- 所有数据必须在 `sources.md` 登记来源和获取方式，防止答辩被问住。
- **接口先于实现**：`docs/仿真接口约定.md` 未定稿前，ns-3 / Python / Web 三方不得各自开工写对接代码。
- Git 管理代码，AI 每完成一个模块提交一次，commit 注明"AI 生成 + 人工验收"。

---

## 七、第一阶段冲刺计划（7 周）

> 本表为**项目启动阶段的 7 周冲刺计划**，对应《计划书》阶段一（2026.9–10 文献调研与场景建模）与阶段二（2026.11–12 架构设计与平台搭建）。
> 后续阶段三~六（基线复现 → 协议实现 → 对比实验 → 结题）按《计划书》进度表滚动更新。
> **仿真选型已定稿为 ns-3 + Python + Web 三栈分工，不再存在"选型决策"环节。**

| 周 | 目标 | AI 任务（工具） | 人工任务 |
|----|------|------------------|----------|
| 1 | 双轨环境就绪 + **接口定稿** | A1 搜集星历/文献（搜索工具）；A2 搭 ns-3 + Python 骨架（venv） | 环境验收、**敲定 `docs/仿真接口约定.md` 字段表** |
| 2 | 场景与轨道建模 | A2 实现 T2 轨道模型与灾害场景参数包（skyfield/sgp4 + pandas） | 灾害场景参数取值、终端分布假设确认 |
| 3~4 | 接入握手 + **安全认证** | A2 实现 T3/T4（venv + 单元测试 + 伪造终端攻击脚本） | **协议与认证方案设计（人主导）**、代码评审 |
| 5 | 预测切换 + 决策 + 重连 | A2 实现 T5/T6/T7（venv） | 预测窗口、切换阈值、比对字段确认 |
| 6 | 业务保障 + 空基开关 | A2 实现 T8；T9 可配置空基模块（**默认关闭**） | 决定是否启用空基、参数取值 |
| 7 | 基线对比 + 首轮出图 | A3 跑 baseline 对比、出图（matplotlib） | 结果分析、问题定位 |

---

## 八、风险与预案

| 风险 | 等级 | 预案 |
|------|------|------|
| AI 生成代码有隐性 bug | 中 | 每模块强制单元测试 + 与 baseline 对拍 |
| AI 编造数据（幻觉） | 中 | T10 数据必须给出可访问的真实 URL；无源数据一律拒收 |
| 工具选型错误拖慢进度 | 中 | 任务卡锁死工具；发现绕路立即纠正并更新本文档框架 |
| 搜集数据来源不可靠 | 中 | sources.md 溯源 + 两个以上来源交叉验证 |
| 环境污染 | 低 | Python/Node 包只装受管环境，定期清理 |
| 创新点被 AI 带偏 | 高 | 核心设计文档先写死，AI 只按设计实现（任务卡注明"只实现不改方案"） |
| 现场演示翻车 | 中 | 预录完整演示视频兜底 |
| **ns-3 学习曲线陡峭、改动接入流程源码门槛高** | **高** | ① 第 1 周即开始环境搭建，预留学习时间；② 优先用现成卫星/NTN 扩展模块，仅在参数与流程逻辑层改动；③ 受阻则退化为 Python 侧建模复现，保证数据产出不中断 |
| **认证机制拖慢接入时延（安全与快速冲突）** | **高** | 认证须满足"不增握手轮次、不显著增信令、星上计算轻量"三重约束；把「认证引入额外时延」列为独立指标，超标即回退简化方案 |
| **三栈接口对接返工** | **中** | T1 阶段先产出 `docs/仿真接口约定.md` 并定死字段表；任何字段变更须同步更新该文档并通知三方 |
| **空基段扩边导致工作量失控** | **中** | 定位为可开关弹性层，v1 默认关闭；只做配置化节点建模（数量/半径/驻留时长）；进度紧张时整体裁剪，不影响主线 |
| **预测失配（实际可见卫星与预测卫星不一致）** | **中** | 设计失配回退机制（回退常规接入流程）；把预测失配率作为专门实验变量，用参数化实验刻画其影响 |
| **优化方案性能提升不达预期** | **中** | 指标采用"相对基线的提升百分比"表述，弱化绝对值；接入/切换/调度/安全四方向各自独立出结果，保证至少两个方向出效果 |
